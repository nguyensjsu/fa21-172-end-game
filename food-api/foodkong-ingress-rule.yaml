apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: food-api
  namespace: default
spec:
  ingressClassName: kong
  rules:
  - http:
      paths:
      - path: /test
        pathType: Prefix
        backend:
          service:
            name: spring-food-api-service
            port:
              number: 80
#apiVersion: extensions/v1beta1
#kind: Ingress
#metadata:
#  name: food-api
#  annotations:
#    kubernetes.io/ingress.class: kong
#spec:
#  rules:
#  - http:
#      paths:
#      - path: /api
#        backend:
#          serviceName: spring-food-api-service
#          servicePort: 80              