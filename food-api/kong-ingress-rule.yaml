apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: food-api
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
  - http:
      paths:
      - path: /api
        backend:
          serviceName: spring-food-api-service
          servicePort: 80